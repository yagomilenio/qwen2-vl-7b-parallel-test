# ─────────────────────────────────────────────────────────────────
#  model_config.toml — Config interna de run_vision.py
#  El servidor de paralelización NO lee este fichero.
# ─────────────────────────────────────────────────────────────────

[model]
id             = "qwen2.5-vl-7b"
temperature    = 0.7
max_tokens     = 1024
context_length = 4096
timeout_sec    = 180
max_retries    = 3
retry_delay    = 5.0
